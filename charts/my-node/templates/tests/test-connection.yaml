apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-node.fullname" . }}-test-connection"
  labels:
    {{- include "my-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

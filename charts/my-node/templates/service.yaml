apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-node.fullname" . }}
  labels:
    {{- include "my-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-node.selectorLabels" . | nindent 4 }}
